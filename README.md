## What I Learned

- How to calculate a weighted average using percentage multipliers (`midterm * 40 / 100`)
- How to combine two weighted components into a single result
- How to use an `if / else` to classify a numeric result against a pass threshold
- How integer division in C truncates decimals, which affects the precision of the calculated average
